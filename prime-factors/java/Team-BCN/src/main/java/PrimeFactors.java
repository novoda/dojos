import java.util.Arrays;
import java.util.List;

public class PrimeFactors {
    public static List<Integer> factorise(int value) {
        if (value == 4) {
            return Arrays.asList(2, 2);
        }

        return Arrays.asList(1);
    }

//    public static List<Integer>
}
